# Shared code assets

This folder will hold reusable helpers and mock data generators shared across session snapshots.

## Planned contents

- mock market data generator
- shared finance model helpers
- reusable configuration patterns

## Guardrail

Keep evolving session-specific agent code inside each `session-0X/code` folder.

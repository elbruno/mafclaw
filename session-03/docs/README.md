# Session 03 Docs

Session 03 scales the claw with orchestration-oriented placeholders.

Target framework: .NET 9.

## What this sample shows

- a local skill catalog for readable demo skills
- placeholder shell execution flow with safe output
- mock background-task queuing without long-lived processes
- a compact orchestration summary suitable for a CodeAct-style walkthrough

## Run the snapshot

From `session-03\code`:

```powershell
dotnet run --project .\MafClaw.Session03.csproj
```

The sample is self-contained, uses placeholder execution only, and is not financial advice.

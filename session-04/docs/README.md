# Session 04 Docs

Session 04 packages the claw with observability and governance placeholders.

Target framework: .NET 9.

## What this sample shows

- placeholder observability settings loaded from local JSON
- a governance checklist for safe demo publishing
- deployment-oriented configuration with mock values only
- a final snapshot summary suitable for the end-of-series walkthrough

## Run the snapshot

From `session-04\code`:

```powershell
dotnet run --project .\MafClaw.Session04.csproj
```

The sample is self-contained, uses safe placeholder configuration only, and is not financial advice.
